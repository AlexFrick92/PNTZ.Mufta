<Window x:Class="PNTZ.Mufta.Showcase.TestWindows.EditRecipeViewTestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:recipe="clr-namespace:PNTZ.Mufta.TPCApp.View.Recipe;assembly=PNTZ.Mufta.TPCApp"
        Title="Тестирование EditRecipeView"
        Height="600" Width="1000"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
    <!-- Заголовок -->
        <Border Grid.Row="0"
                Background="#F8F9FA"
                BorderBrush="#DEE2E6"
                BorderThickness="0,0,0,1"
                Padding="15">
            <StackPanel>
                <TextBlock Text="EditRecipeView - Тестирование редактирования рецепта"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="#2C3E50"
                           Margin="0,0,0,10"/>

                <WrapPanel>
                    <!-- Группа: Тестовые рецепты -->
                    <GroupBox Header="Тестовые рецепты"
                              Margin="0,0,15,10"
                              Padding="10">
                        <StackPanel Orientation="Horizontal">
                            <Button x:Name="BtnLoadRecipeLength"
                                    Content="Length"
                                    Click="BtnLoadRecipeLength_Click"
                                    Width="100"
                                    Margin="0,0,5,0"
                                    Padding="8,5"
                                    Background="#3498DB"
                                    Foreground="White"
                                    BorderThickness="0"
                                    Cursor="Hand"/>
                            <Button x:Name="BtnLoadRecipeTorque"
                                    Content="Torque"
                                    Click="BtnLoadRecipeTorque_Click"
                                    Width="100"
                                    Margin="0,0,5,0"
                                    Padding="8,5"
                                    Background="#9B59B6"
                                    Foreground="White"
                                    BorderThickness="0"
                                    Cursor="Hand"/>
                            <Button x:Name="BtnLoadRecipeTorqueLength"
                                    Content="Torque+Length"
                                    Click="BtnLoadRecipeTorqueLength_Click"
                                    Width="130"
                                    Margin="0,0,5,0"
                                    Padding="8,5"
                                    Background="#E67E22"
                                    Foreground="White"
                                    BorderThickness="0"
                                    Cursor="Hand"/>
                            <Button x:Name="BtnLoadRecipeTorqueShoulder"
                                    Content="Torque+Shoulder"
                                    Click="BtnLoadRecipeTorqueShoulder_Click"
                                    Width="150"
                                    Padding="8,5"
                                    Background="#16A085"
                                    Foreground="White"
                                    BorderThickness="0"
                                    Cursor="Hand"/>
                        </StackPanel>
                    </GroupBox>

                    <!-- Группа: Действия -->
                    <GroupBox Header="Действия"
                              Margin="0,0,15,10"
                              Padding="10">
                        <StackPanel Orientation="Horizontal">
                            <Button Content="💾 Сохранить рецепт"
                                    Command="{Binding ElementName=EditRecipeView, Path=DataContext.SaveRecipeCommand}"
                                    Width="150"
                                    Margin="0,0,5,0"
                                    Padding="8,5"
                                    Background="#27AE60"
                                    Foreground="White"
                                    BorderThickness="0"
                                    Cursor="Hand"/>
                            <Button Content="↩️ Отменить изменения"
                                    Command="{Binding ElementName=EditRecipeView, Path=DataContext.CancelCommand}"
                                    Width="170"
                                    Padding="8,5"
                                    Background="#E74C3C"
                                    Foreground="White"
                                    BorderThickness="0"
                                    Cursor="Hand"/>
                        </StackPanel>
                    </GroupBox>
                </WrapPanel>

                <!-- Статус -->
                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                    <Border Background="#D5F4E6"
                            BorderBrush="#27AE60"
                            BorderThickness="1"
                            CornerRadius="3"
                            Padding="8"
                            Margin="0,0,10,0">
                        <TextBlock x:Name="StatusText"
                                   Text="Готов к тестированию."
                                   FontSize="12"
                                   Foreground="#27AE60"/>
                    </Border>

                    <!-- Индикатор изменений -->
                    <Border Padding="8"
                            CornerRadius="3"
                            BorderThickness="1">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="#E8F5E9"/>
                                <Setter Property="BorderBrush" Value="#4CAF50"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ElementName=EditRecipeView, Path=DataContext.HasChanges}" Value="True">
                                        <Setter Property="Background" Value="#FFF3E0"/>
                                        <Setter Property="BorderBrush" Value="#FF9800"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <TextBlock FontSize="12" FontWeight="Bold">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value="✓ Нет изменений"/>
                                    <Setter Property="Foreground" Value="#4CAF50"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ElementName=EditRecipeView, Path=DataContext.HasChanges}" Value="True">
                                            <Setter Property="Text" Value="⚠ Есть несохранённые изменения"/>
                                            <Setter Property="Foreground" Value="#FF9800"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Border>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Контрол для тестирования -->
        <Border Grid.Row="1"
                BorderBrush="#DEE2E6"
                BorderThickness="1,0,1,1"
                Margin="15"
                Background="White">
            <recipe:EditRecipeView x:Name="EditRecipeView"/>
        </Border>
    </Grid>
</Window>
