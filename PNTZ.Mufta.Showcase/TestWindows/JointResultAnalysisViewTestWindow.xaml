<Window x:Class="PNTZ.Mufta.Showcase.TestWindows.JointResultAnalysisViewTestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:joint="clr-namespace:PNTZ.Mufta.TPCApp.View.Joint;assembly=PNTZ.Mufta.TPCApp"
        Title="Тестирование JointResultAnalysisView"
        Height="800" Width="1400"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Левая панель: управление и список результатов -->
        <Border Grid.Column="0"
                Background="#F8F9FA"
                BorderBrush="#DEE2E6"
                BorderThickness="0,0,1,0"
                Padding="15">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Заголовок -->
                <TextBlock Grid.Row="0"
                           Text="Анализ результатов свинчивания"
                           FontSize="18"
                           FontWeight="Bold"
                           Foreground="#2C3E50"
                           Margin="0,0,0,15"/>

                <!-- Кнопка загрузки БД -->
                <GroupBox Grid.Row="1"
                          Header="База данных"
                          Margin="0,0,0,15"
                          Padding="10">
                    <StackPanel>
                        <Button x:Name="BtnLoadDatabase"
                                Content="📂 Загрузить БД"
                                Click="BtnLoadDatabase_Click"
                                Padding="10,8"
                                Background="#3498DB"
                                Foreground="White"
                                BorderThickness="0"
                                Cursor="Hand"
                                Margin="0,0,0,8"/>

                        <TextBlock x:Name="DatabaseStatusText"
                                   Text="База данных не загружена"
                                   FontSize="11"
                                   Foreground="#7F8C8D"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                </GroupBox>

                <!-- Список результатов -->
                <GroupBox Grid.Row="2"
                          Header="Результаты свинчивания"
                          Padding="5">
                    <ListBox x:Name="ResultsListBox"
                             SelectionChanged="ResultsListBox_SelectionChanged"
                             Background="White"
                             BorderThickness="0">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="5">
                                    <TextBlock Text="{Binding Name}"
                                               FontWeight="Bold"
                                               FontSize="13"
                                               Foreground="#2C3E50"/>
                                    <TextBlock FontSize="11" Foreground="#7F8C8D">
                                        <Run Text="Дата: "/>
                                        <Run Text="{Binding FinishTimeStamp, StringFormat='dd.MM.yyyy HH:mm:ss'}"/>
                                    </TextBlock>
                                    <TextBlock FontSize="11" Foreground="#7F8C8D">
                                        <Run Text="Момент: "/>
                                        <Run Text="{Binding FinalTorque, StringFormat='{}{0:F1} Nm'}"/>
                                        <Run Text=" | Длина: "/>
                                        <Run Text="{Binding FinalLength, StringFormat='{}{0:F3} м'}"/>
                                    </TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </GroupBox>
            </Grid>
        </Border>

        <!-- Правая панель: контрол для тестирования -->
        <Border Grid.Column="1"
                Background="White"
                Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Статус -->
                <Border Grid.Row="0"
                        Background="#D5F4E6"
                        BorderBrush="#27AE60"
                        BorderThickness="1"
                        CornerRadius="3"
                        Padding="10"
                        Margin="0,0,0,15">
                    <TextBlock x:Name="StatusText"
                               Text="Готов к тестированию. Загрузите базу данных и выберите результат."
                               FontSize="12"
                               Foreground="#27AE60"/>
                </Border>

                <!-- Контрол для тестирования -->
                <Border Grid.Row="1"
                        BorderBrush="#DEE2E6"
                        BorderThickness="1"
                        Background="White">
                    <joint:JointResultAnalysisView x:Name="AnalysisView"/>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
