<Window x:Class="PNTZ.Mufta.TPCApp.View.Recipe.LoadingRecipeView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PNTZ.Mufta.TPCApp.View.Recipe"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"

        xmlns:viewmodel="clr-namespace:PNTZ.Mufta.TPCApp.ViewModel.Recipe"
        d:DataContext="{d:DesignInstance Type=viewmodel:LoadingRecipeViewModel}"

        Title="LoadingRecipeView" Height="150" Width="350">

    <Border BorderBrush="DarkGray" BorderThickness="3">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBox
                Grid.Row="0"
                IsReadOnly="True"
                BorderThickness="0"
                FontSize="14"
                TextAlignment="Center"
                Margin="0,0,0,10"
                Text="Загрузка рецепта:"/>

            <TextBox
                Grid.Row="1"
                IsReadOnly="True"
                HorizontalAlignment="Center"
                BorderThickness="0"
                FontSize="16"
                FontWeight="Bold"
                Margin="0,0,0,15"
                Text="{Binding RecipeName}"/>

            <ProgressBar
                Grid.Row="2"
                Height="20"
                Margin="20,0,20,10"
                IsIndeterminate="True"/>

            <TextBox
                Grid.Row="3"
                IsReadOnly="True"
                BorderThickness="0"
                HorizontalAlignment="Center"
                VerticalAlignment="Top"
                FontStyle="Italic"
                Foreground="Gray"
                Text="Загружается..."/>

        </Grid>
    </Border>
</Window>
