<UserControl x:Class="PNTZ.Mufta.TPCApp.View.Joint.JointProcessChartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PNTZ.Mufta.TPCApp.View.Joint"
             xmlns:ctrl="clr-namespace:PNTZ.Mufta.TPCApp.View.Control"
             xmlns:vmJoint="clr-namespace:PNTZ.Mufta.TPCApp.ViewModel.Joint"
             d:DataContext="{d:DesignInstance Type=vmJoint:JointProcessChartViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1000">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!-- Момент/обороты -->
        <ctrl:ChartView Grid.Row="0" Grid.Column="0" DataContext="{Binding TorqueTurnsChart}"/>

        <!-- (Обороты/Мин)/обороты -->
        <ctrl:ChartView Grid.Row="1" Grid.Column="0" DataContext="{Binding TurnsPerMinuteTurnsChart}"/>

        <!-- Момент/длина -->
        <ctrl:ChartView Grid.Row="0" Grid.Column="1" DataContext="{Binding TorqueLengthChart}"/>
        
        <!-- Момент/время -->
        <ctrl:ChartView Grid.Row="1" Grid.Column="1" DataContext="{Binding TorqueTimeChart}"/>


    </Grid>
</UserControl>
