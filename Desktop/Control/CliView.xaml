<UserControl x:Class="Desktop.Control.CliView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Desktop.Control"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Desktop;component/Styles/LayoutBlueStyle.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Background="{StaticResource SecondBgBrush}">
        <DockPanel LastChildFill="True" Margin="5" Background="{StaticResource SecondBgBrush}" >
            <Grid DockPanel.Dock="Bottom">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Label Content=">" FontWeight="Bold" FontSize="20" Tag="Label" Foreground="{StaticResource TextColorBrush}" VerticalAlignment="Center" />
                <TextBox Background="{StaticResource SecondBgBrush}" Tag ="Cli" x:Name="Input" Text="{Binding Input, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"  Grid.Column="1" VerticalAlignment="Bottom" BorderThickness="0" Height="30" VerticalContentAlignment="Center">
                    <TextBox.InputBindings>
                        <KeyBinding 
                            Key="Enter" 
                            Command="{Binding EnterInput}" 
                            CommandParameter="{Binding Path=Text, RelativeSource={RelativeSource AncestorType={x:Type TextBox}}}" />
                    </TextBox.InputBindings>
                </TextBox>
            </Grid>
            <ScrollViewer x:Name="OutputScroll">
                <TextBlock Tag ="Cli" Text="{Binding Output}" x:Name="Output" MouseLeftButtonUp="Output_MouseLeftButtonUp" GotFocus="Output_GotFocus" TextWrapping="Wrap"/>
            </ScrollViewer>
        </DockPanel>
    </Grid>
</UserControl>
