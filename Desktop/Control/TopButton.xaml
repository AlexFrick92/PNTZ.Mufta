<UserControl x:Class="Desktop.Control.TopButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Desktop.Control"
             mc:Ignorable="d" 
             x:Name="this"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Desktop;component/Styles/LayoutBlueStyle.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/Desktop;component/Styles/Buttons.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Button Margin="3,3,3,3" Style="{StaticResource NaviButton}" Command="{Binding ButtonCommand, ElementName=this}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <Label Style="{StaticResource NaviButtonTopLabel}" Grid.Row="0" Content="{Binding TopLabel, ElementName=this}" />
                <Image Grid.Row="1" Source="{Binding IconImage, ElementName=this}" />
                <Label Style="{StaticResource NaviButtonLabel}" Grid.Row="2" Content="{Binding BottomLabel, ElementName=this}" />
            </Grid>
        </Button>
    </Grid>
</UserControl>
