<UserControl x:Class="Desktop.Control.OutputBarButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Desktop.Control"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             x:Name="this"
             >
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Desktop;component/Styles/LayoutBlueStyle.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid Margin="5,0,5,0">
        <Border BorderBrush="{StaticResource TextColorBrush}">
            <Border.Style>
                <Style TargetType="Border">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Active, ElementName=this}" Value="True">
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Active, ElementName=this}" Value="False">
                            <Setter Property="BorderThickness" Value="0,0,0,0"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>

            <Button Style="{StaticResource ContentButton}" Command="{Binding Switch, ElementName=this}">
                <Label Margin="3,0,3,0" Content="{Binding Header, ElementName=this}"/>

            </Button>
        </Border>
    </Grid>
</UserControl>
